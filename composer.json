{
  "name": "scratchbyphp/scratchbyphp",
  "description": "A bridge between PHP applications and Scratch: projects, users, studios, auth, Cloud Variables, analysis, CLI and developer tooling.",
  "type": "library",
  "license": "MIT",
  "keywords": [
    "scratch",
    "scratch-api",
    "scratch-cloud",
    "cloud-variables",
    "php",
    "websocket",
    "scratch-mit",
    "scratch3"
  ],
  "homepage": "https://www.blocklandin.com/scratchbyphp/",
  "support": {
    "issues": "https://github.com/scratchbyphp/scratchbyphp/issues",
    "source": "https://github.com/scratchbyphp/scratchbyphp"
  },
  "autoload": {
    "psr-4": {
      "ScratchByPHP\\": "src/"
    }
  },
  "require": {
    "php": ">=8.1",
    "ext-curl": "*",
    "ext-openssl": "*",
    "ext-json": "*"
  },
  "require-dev": {
    "phpunit/phpunit": "^10.5 || ^11.0",
    "phpstan/phpstan": "^1.12 || ^2.0"
  },
  "scripts": {
    "lint": "php -r \"$it=new RecursiveIteratorIterator(new RecursiveDirectoryIterator('src')); foreach($it as $f){if($f->isFile()&&$f->getExtension()==='php'){passthru('php -l '.escapeshellarg($f->getPathname()),$c); if($c) exit($c);}}\"",
    "test": "phpunit",
    "analyse": "phpstan analyse",
    "security": "php tests/security.php",
    "api-docs": "php tools/generate-api.php"
  },
  "minimum-stability": "stable",
  "prefer-stable": true,
  "bin": [
    "bin/scratchbyphp"
  ]
}
