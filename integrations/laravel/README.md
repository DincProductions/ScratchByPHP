# ScratchByPHP Laravel Bridge
Optional integration skeleton planned as a separate Composer package. The core library remains usable directly from PHP. This folder is not included as an automatic runtime dependency.
