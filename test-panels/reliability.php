<?php declare(strict_types=1);require dirname(__DIR__).'/autoload.php';use ScratchByPHP\Scratch;use ScratchByPHP\Http\RetryPolicy;use ScratchByPHP\Http\CircuitBreaker;$s=new Scratch();$s->cacheRules(['project:'=>15,'user:'=>120,'studio:'=>60]);$p=$s->retry()->maxAttempts(3)->backoff('exponential')->baseDelayMs(100)->retryOn([429,500,502,503]);$cb=$s->circuitBreaker()->threshold(3)->cooldown(10);$result=['version'=>$s::VERSION,'retry_policy'=>$p->toArray(),'circuit'=>$cb->state(),'cache_rules_test'=>'configured','metrics'=>$s->metrics()->summary(),'doctor_local'=>$s->healthCheck(false)];?><!doctype html><meta charset="utf-8"><title>Reliability v0.8.5</title><link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"><div class="container py-4"><a href="index.php">← Test Merkezi</a><h1 class="h3 mt-3">Reliability / DX 2.0</h1><pre class="bg-dark text-light p-3 rounded"><?=htmlspecialchars(json_encode($result,JSON_PRETTY_PRINT|JSON_UNESCAPED_UNICODE|JSON_UNESCAPED_SLASHES))?></pre></div>